(ns copycat-clojure.core-test
  (:use [clojure.test]
        [copycat-clojure.core]))

;; (load "formulas_test")

;; run with 'lein test'

(deftest a-test
  (testing
   "Should pass"
   (is (= 1 1))
   ;; (is (blah 2 2) 4)
   ;; (is false)
   )
  )

